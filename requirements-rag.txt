# RAG (Retrieval-Augmented Generation) ML Dependencies
# Only install these if working on RAG features (heavy packages ~2GB)
# 
# INSTALLATION OPTIONS:
# 1. Full RAG: pip install -r requirements-rag.txt (recommended)
# 2. Minimal:  pip install -r requirements-rag-minimal.txt (smaller, auto-installs torch)
# 3. Manual:   pip install sentence-transformers (simplest)
#
# The RAG system has fallbacks - it works even if these fail to install!

# Core ML/AI packages (flexible versions for compatibility)
torch>=2.1.0,<3.0.0
torchvision>=0.16.0,<1.0.0  
torchaudio>=2.1.0,<3.0.0

# Sentence transformers for embeddings
sentence-transformers>=2.2.0,<3.0.0

# Additional ML utilities (flexible versions)
transformers>=4.30.0,<5.0.0
safetensors>=0.4.0,<1.0.0
accelerate>=0.20.0,<1.0.0

# Scientific computing (if not already in base requirements)
scikit-learn>=1.3.0,<2.0.0
